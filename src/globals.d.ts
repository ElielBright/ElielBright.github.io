// Ambient declarations for side-effect style imports (e.g. `import "./globals.css";`).
// Resolves TS 2882 in editors whose TS server doesn't load the Next.js plugin types.
declare module "*.css";
declare module "*.scss";
